<h1>Moods</h1>
<table>
	<tr>
		<th>Name</th>
		<th>Image Filename</th>
	</tr>
	
	<% Mood.all.each do |mood| %>
	<tr>
		<td><%= mood.name %></td>
		<td><%= mood.image_filename %></td>
		<td><%= link_to 'Show', mood %></td>
		<td><%= link_to 'Edit', edit_mood_path(mood) %></td>
		<td><%= link_to 'Destroy', mood, method: :delete, data: { confirm: 'Are you sure?' }%></td>
	</tr>
	<% end %>
</table>
